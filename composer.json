{
    "name": "lost-and-found/backend",
    "description": "Lost and Found System Backend API",
    "type": "project",
    "require": {
        "php": ">=7.4"
    },
    "require-dev": {
        "phpunit/phpunit": "^9.0"
    },
    "autoload": {
        "psr-4": {
            "Tests\\": "tests/"
        }
    },
    "scripts": {
        "test": "phpunit tests/",
        "test-unit": "phpunit tests/unit/",
        "test-coverage": "phpunit tests/ --coverage-html coverage/"
    }
}